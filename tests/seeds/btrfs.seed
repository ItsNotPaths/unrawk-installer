# Btrfs variant of basic.seed. Same fields except filesystem=btrfs, so
# the diff between basic.txt and btrfs.txt isolates the mkfs + fstab
# branch.

keyboard        = us
timezone        = America/New_York
hostname        = unrawk-btrfs
password        = hunter2
luks_passphrase = openthebox
disk            = /dev/sda
filesystem      = btrfs
