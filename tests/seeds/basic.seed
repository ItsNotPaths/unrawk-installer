# Canonical seed for the headless / golden-file harness.
# One key=value per line, `#` comments + blank lines ignored.
# See src/runmode.nim parseSeed for accepted keys.

keyboard        = us
timezone        = America/New_York
hostname        = unrawk-test
password        = hunter2
luks_passphrase = openthebox
disk            = /dev/sda
filesystem      = ext4
